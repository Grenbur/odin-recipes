# odin-recipes

This is the first project that actually uses git. Using git seems intimidating.

The goal is to make a simple recipe website. It should have a main index page which links to a few pages that have recipes on them. Succesful completion will show the HTML knowledge that I have acquired thus far (lists, formatting, images, links etc)